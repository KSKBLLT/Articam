# 🏺 ARTICAM - Archaeological Analyzer

Профессиональный анализатор артефактов с Claude AI в стиле National Geographic.

## ✅ Что включено:

- ✅ Vercel Serverless Function с полной обработкой ошибок
- ✅ React UI с валидацией файлов
- ✅ Полная обработка всех возможных ошибок
- ✅ CORS настроен правильно
- ✅ API ключ встроен и работает
- ✅ Мобильный + десктоп дизайн

## 🚀 ДЕПЛОЙ НА VERCEL (3 минуты):

```bash
# 1. Откройте терминал в папке articam-final

git init
git add .
git commit -m "Deploy Articam"
git branch -M main
git remote add origin https://github.com/KSKBLLT/Articam.git
git push -u origin main --force
```

Vercel автоматически:
1. Обнаружит проект
2. Соберёт фронтенд
3. Развернёт `/api/analyze.js` как serverless function
4. Выдаст ссылку на сайт

**Ждите 2-3 минуты - готово!**

## 📁 Структура проекта:

```
articam-final/
├── api/
│   └── analyze.js          # Serverless function (обходит CORS)
├── src/
│   ├── App.jsx             # React UI
│   ├── main.jsx            
│   └── index.css           
├── index.html              
├── package.json            
└── ...configs
```

## 🔧 Как работает:

```
1. Пользователь загружает фото
   ↓
2. React отправляет на /api/analyze
   ↓
3. Serverless function получает запрос
   ↓
4. Function отправляет в Claude API
   ↓
5. Claude анализирует изображение
   ↓
6. Function возвращает JSON
   ↓
7. React показывает результат
```

## ✅ Обработка ошибок:

- ❌ Нет изображения - показываем ошибку
- ❌ Файл слишком большой (>10MB) - предупреждаем
- ❌ Не изображение - отклоняем
- ❌ Ошибка сети - показываем сообщение
- ❌ Ошибка API - показываем детали
- ❌ Неверный JSON - обрабатываем
- ✅ Всё работает - показываем результат!

## 🧪 Тестирование:

После деплоя:
1. Откройте сайт
2. Загрузите любое фото
3. Нажмите Analyze
4. Получите результат!

Если ошибка - на экране будет показано ЧТО именно пошло не так.

## 💰 Стоимость:

- Vercel: Бесплатно
- Claude API: $5 на балансе (~1500 анализов)

## 📱 Мобильная версия:

Автоматически адаптируется под телефоны и планшеты.

---

**Powered by Claude AI · National Geographic Style**
